'use client';

import React from 'react';
import { useAuthContext } from '@/context/AuthContext';

const Admin = () => {
  // const credentials: object = useAuthContext();
  // console.log(credentials);

  return <div>Admin</div>;
};

export default Admin;
